// ========================================================================
//  НАСТРОЙКА ПРОЕКТА
//  Заполните эти значения после создания проекта Supabase (см. SETUP.md).
//  Пока поля пустые — система работает в ЛОКАЛЬНОМ демо-режиме (данные
//  хранятся в браузере), чтобы можно было сразу всё посмотреть и потестить.
// ========================================================================

window.APP_CONFIG = {
  // --- Supabase (оставьте пустыми для локального режима) ---
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",

  // --- Доступ к админке ---
  // ВНИМАНИЕ: всё в этом файле попадает в браузер и НЕ является секретом.
  // Реальная защита админки — вход через Supabase Auth (email+пароль) и
  // серверная проверка JWT в эндпоинтах /api/*. Значения ниже — лишь
  // удобство/обфускация, а не граница безопасности.
  //
  // Секретный слаг ссылки (просто «прячет» страницу, не защищает её):
  //   index.html#/?key=СЕКРЕТ
  SECRET_KEY: "sklad-39d09dddfa897be5",
  // Пароль ЛОКАЛЬНОГО демо-режима (без Supabase). В онлайне НЕ используется —
  // там вход только через Supabase Auth. На боевом сайте оставьте пустым.
  ADMIN_PASSWORD: "",

  // --- Telegram ---
  // Адрес serverless-функции отправки (на Vercel это "/api/telegram").
  // Токен бота НЕ хранится здесь — он лежит в переменных окружения Vercel.
  TELEGRAM_API: "/api/telegram",
  TELEGRAM_CHANNEL: "",      // @username канала или -100... id (для накладных)

  // --- Каталог ---
  CATALOG_URL: "catalog.html", // ссылка на публичный каталог

  // --- Валюты по умолчанию ---
  DEFAULT_RATES: {
    yuan_usd: 0.14,   // 1 юань = ? долларов
    som_usd: 0.000079 // 1 сум  = ? долларов  (≈ 12650 сум за $1)
  }
};
