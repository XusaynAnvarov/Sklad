-- ========================================================================
--  Колонка для канала накладных. Без неё «Сохранить канал» не сохраняется
--  и при отправке выходит «Не указан канал».
--  Запустить ОДИН раз в Supabase → SQL Editor.
-- ========================================================================
alter table if exists settings add column if not exists telegram_channel text;
