-- ========================================================================
--  «Доверенные устройства»: код из Telegram спрашиваем ТОЛЬКО при входе с нового
--  устройства. На известном устройстве — вход только по паролю (без кода).
--  Запустить ОДИН раз в Supabase → SQL Editor.
-- ========================================================================
alter table if exists site_accounts add column if not exists known_devices jsonb default '[]'::jsonb;
